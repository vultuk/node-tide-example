import * as Entities from "../entities";
import { Transformer } from "../transformers/transformer";
export declare class Credentials extends Transformer {
    item(item: any): Promise<Entities.Credentials>;
}
