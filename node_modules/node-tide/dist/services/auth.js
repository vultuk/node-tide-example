"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var Transformers = require("../transformers");
var service_1 = require("../services/service");
var Auth = (function (_super) {
    __extends(Auth, _super);
    function Auth() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    Auth.prototype.generateAuthUrl = function (config) {
        return "https://api.tide.co/tide-backend/oauth/index.html?redirect_url=" + config.redirectUrl + "&client_id=" + config.clientId;
    };
    Auth.prototype.generateAccessToken = function (code, refresh) {
        var _this = this;
        if (refresh === void 0) { refresh = false; }
        return Promise
            .resolve({ code: code, refresh: refresh })
            .then(function (_a) {
            var code = _a.code, refresh = _a.refresh;
            return _this.getRequest("/oauth2/tokens?" + (refresh ? 'refresh_token' : 'code') + "=" + code, {});
        })
            .then(function (response) { return (new Transformers.Credentials).item(response); });
    };
    return Auth;
}(service_1.Service));
exports.Auth = Auth;
//# sourceMappingURL=auth.js.map