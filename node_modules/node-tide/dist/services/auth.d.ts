import * as Entities from "../entities";
import { Service } from "../services/service";
export declare class Auth extends Service {
    generateAuthUrl(config: Entities.AuthenticationConfig): string;
    generateAccessToken(code: string, refresh?: boolean): Promise<Entities.Credentials>;
}
