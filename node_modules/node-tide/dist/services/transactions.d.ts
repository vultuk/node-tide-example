import * as Entities from "../entities";
import * as moment from "moment";
import { Service } from "../services/service";
export declare class Transactions extends Service {
    all(accountId: number): Promise<Entities.Transaction[]>;
    findReference(accountId: number, transactionReference: string): Promise<Entities.Transaction[]>;
    findDateRange(accountId: number, startDate: moment.Moment, endDate?: moment.Moment): Promise<Entities.Transaction[]>;
}
